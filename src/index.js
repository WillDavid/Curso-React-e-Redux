import ReactDOM from 'react-dom';
import React from 'react';
import './index.css';

import App from './App';

// Renderizar o Olá dentro do elemento (root)
ReactDOM.render(
    <App></App>, 
    document.getElementById('root')
)
